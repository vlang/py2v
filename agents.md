# Agent Rules

## Before Committing

- Always run `v fmt -w .` before creating a commit to ensure correct V formatting.

## Testing

- Always run tests on Linux as well as the local platform. Use the project's CI or cross-platform test scripts to verify Linux compatibility.
